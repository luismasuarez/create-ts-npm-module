{
  "name": "create-ts-npm-module",
  "version": "0.0.1",
  "description": "My custom React scaffold",
  "main": "index.js",
  "bin": {
    "create-ts-npm-module": "./index.js"
  },
  "scripts": {
    "prepublishOnly": "node -e \"const fs = require('fs'); const path = require('path'); const npmignorePath = path.join(__dirname, 'template', '.npmignore'); const backupPath = path.join(__dirname, 'template', 'npmignore.bak'); if (fs.existsSync(npmignorePath)) fs.renameSync(npmignorePath, backupPath);\"",
    "postpublish": "node -e \"const fs = require('fs'); const path = require('path'); const backupPath = path.join(__dirname, 'template', 'npmignore.bak'); const npmignorePath = path.join(__dirname, 'template', '.npmignore'); if (fs.existsSync(backupPath)) fs.renameSync(backupPath, npmignorePath);\""
  },
  "keywords": [],
  "author": "Luis Manuel Cauasa Suárez",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^11.2.0",
    "inquirer": "^8.1.2"
  }
}
